# Example env for server
# set DATABASE_URL or PG_CONNECTION
# Example connection string using default user/password (password defaults to 'root123' if not set):
# DATABASE_URL=postgresql://postgres:root123@localhost:5432/edunexus_db
# Or provide individual pieces:
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=root123
# PGDATABASE=edunexus_db
PORT=4000
JWT_SECRET=change_me
ADMIN_API_KEY=changeme_admin_key
